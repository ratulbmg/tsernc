"// composite": "Disables project references (used for monorepo builds with tsc --build)",
"// declaration": "Generates .d.ts type definition files alongside .js files",
"// declarationMap": "Generates .d.ts.map files for 'Go to Definition' on types",
"// esModuleInterop": "Allows import x from 'module' instead of import * as x from 'module'",
"// forceConsistentCasingInFileNames": "Errors if import casing doesn't match file casing (important for cross-OS)",
"// inlineSources": "Don't embed source code in sourcemaps (keeps files smaller)",
"// module": "Output module format - ESNext uses modern ES modules (import/export)",
"// noUnusedLocals": "Don't error on unused local variables (less strict)",
"// noUnusedParameters": "Don't error on unused function parameters (less strict)",
"// preserveWatchOutput": "Don't clear console on rebuild in watch mode",
"// skipLibCheck": "Skip type checking of .d.ts files (faster builds)",
"// strict": "Enables all strict type checking options (recommended)",
"// exclude": "Folders to ignore when compiling",