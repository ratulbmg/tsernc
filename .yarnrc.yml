# node-modules = traditional node_modules folder (instead of Plug'n'Play)
nodeLinker: node-modules

# Enable lifecycle scripts (prepare, postinstall, etc.) so husky runs after yarn install
enableScripts: true